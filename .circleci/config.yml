orbs:
  node: circleci/node@4.2.1

version: 2.1
parameters:
  run_integration_tests:
    type: boolean
    default: $DEPLOY_STATE_FREEZE_v2

workflows:
  integration_tests:
    when: << $DEPLOY_STATE_FREEZE_v2 >>
    jobs:
        - node/test