orbs:
  node: circleci/node@4.2.1

version: 2.1
workflows:
  conditional-workflow:
    when:
      conditional:
        - equal: [ true, true ]
      jobs:
        - node/test
    jobs:
        - node/test